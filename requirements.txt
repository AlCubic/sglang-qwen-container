# SGLang Qwen2.5-0.5B-Instruct Deployment Dependencies
# Python dependencies for local development and model management

# Core dependencies for model interaction
huggingface-hub>=0.19.0
transformers>=4.35.0
torch>=2.0.0
safetensors>=0.4.0

# Additional utilities
requests>=2.31.0
pyyaml>=6.0
tqdm>=4.66.0

# For API testing (optional)
pytest>=7.4.0
pytest-requests>=0.6.0

# For monitoring (optional)
psutil>=5.9.0

# Note: SGLang and its dependencies are included in the Docker image
# lmsysorg/sglang:v0.4.8.post1-cu126
